# Future GUI Applications

This directory is reserved for native Linux, Windows, and macOS GUI applications. GUI implementations are outside Phase 1 and will invoke the CLI rather than duplicate archive logic.
